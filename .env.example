# 프론트엔드(Vite) 환경변수 — 이 파일의 값은 브라우저에 그대로 노출된다.
# GitHub/Vercel 시크릿(GITHUB_CLIENT_SECRET, VERCEL_TOKEN 등)은 여기 두지 말고 server/.env 를 사용할 것.

# 백엔드 API 기본 주소. 로컬 개발 중에는 vite.config.ts의 proxy 설정으로 대체되므로 비워둬도 된다.
VITE_API_BASE_URL=
